> 이 문서는 지도(map)다 — 세부 규칙은 아래 문서에 있다. 코드를 고치기 전에 관련된 문서를 먼저 연다.
>
> - [docs/invariants.md](docs/invariants.md) — 절대 깨면 안 되는 것
> - [docs/commands.md](docs/commands.md) — 빌드·테스트·검증 명령
> - [docs/conventions.md](docs/conventions.md) — 언어별 코드 관례
> - [ARCHITECTURE.md](ARCHITECTURE.md) — 크레이트 의존 그래프·프로세스 경계

## 프로젝트 의도

vidcull은 로컬에 쌓인 대용량 영상 라이브러리에서 중복 영상(완전 동일 / 재인코딩 / 크기·크롭·워터마크 변형 / 부분 클립)을 찾아 정리하는 개인용 Windows·Linux 데스크톱 도구다. **PolyForm Noncommercial 1.0.0** — 비상업 전용 소스 공개 소프트웨어이므로, 상업적 활용(SaaS화, 유료 배포 등)을 전제로 한 제안을 만들지 않는다.

백엔드(Rust 워크스페이스, `crates/`)와 프론트(Tauri 2 + SvelteKit, `app/`)는 로컬 IPC(`vidcull-ipc`)로만 통신하는 별도 프로세스다. 스캔·파싱·지문·매칭·썸네일 같은 무거운 작업은 전부 `vidcull-daemon`이 맡고, UI는 결과만 소비한다. 원본 영상 파일은 옮기거나 복사하지 않고 제자리에서 분석한다.

설치·사용법은 `README.md`, 크레이트 의존 그래프와 프로세스 경계는 `ARCHITECTURE.md`를 따른다. 이 문서와 사실 관계가 어긋나면 그쪽이 기준이다.

## 작업 규칙

- 이 문서는 이 저장소에서 작업하는 모든 AI 코딩 도구(Claude Code, Codex CLI, Antigravity 등)에 공통 적용된다. 그 자리 사용자 지시가 이 문서와 충돌하면 해당 대화에 한해 사용자를 따르되, 이유를 짧게 알린다.
- **`docs/invariants.md`에 있는 규칙은 절대 깨지 않는다** — 지문 결정성(golden bit 테스트), panic 격리(`catch_unwind`), 삭제 안전(휴지통 경유), IPC 버전 게이트, 메타 예산, 네이티브 디코더 수용 범위. 관련 코드를 고치기 전에 먼저 읽는다.
- UI ↔ 데몬 통신은 `vidcull-ipc` 하나만 쓴다. 새 통신 채널이나 이를 우회하는 경로를 만들지 않는다.
- IPC 프로토콜, DB 스키마(`crates/vidcull-db/src/schema/`), 파일 포맷, 설정 키를 바꿀 때는 기존 소비자와의 하위 호환성과 마이그레이션을 먼저 확인한다.
- 여러 크레이트/파일에 걸친 구조 변경, DB 마이그레이션, IPC 프로토콜 버전업처럼 되돌리기 어려운 작업은 구체적인 단계별 계획을 먼저 제시하고 승인받은 뒤 착수한다.
- 새 의존성(Cargo.toml/package.json 추가)이나 프로젝트 설정 변경 전에는 워크스페이스에 이미 있는 대안(`rayon`, `wide` 등)으로 해결되는지 먼저 확인하고, 필요성과 영향 범위를 설명한다.
- 관련 없는 리팩터링, 전체 포맷 변경, 이름 변경을 요청받지 않은 작업과 함께 수행하지 않는다. 사용자의 기존 변경을 덮어쓰거나 되돌리지 않는다.
- 생성 파일과 소스 파일을 구분한다 — `app/dist`, `target/`, `.svelte-kit/` 등 빌드 산출물은 직접 수정하지 않는다.
- 원인이 불분명한 버그나 설계 판단에는 최소 두 개 이상의 가설을 세우고, 실제 코드·로그·재현 결과(가능하면 `cargo bench`/테스트 실측)로 반증하거나 확인한 뒤 남은 가설을 채택한다. 외부에서 제시된 "최적화 제안"은 실제 코드에 해당 심볼·파일이 존재하는지 먼저 확인한 뒤 채택한다.
- 서로 결과 의존성이 없는 조사·검토 작업은 병렬로 수행하고, 한 작업의 출력이 다른 작업의 입력이 되면 순차로 처리한다. 병렬 결과가 서로 상충하면 요약을 그대로 합치지 말고 실제 코드를 다시 확인한다.
- 도구별 커스텀 서브에이전트 이름이나 호출 방식은 이 문서에 적지 않는다 — 각 도구의 개별 설정 위치(Claude Code의 `.claude/agents/*.md` 등)에서 따로 구성한다.
- 수정을 마치면 보고 전에 최소 한 번 스스로 재검토한다: (1) 요구사항을 실제로 충족하는가, (2) 기존 동작을 깨뜨리지 않는가, (3) `docs/invariants.md`를 지키는가.
- 모든 답변과 새 주석은 한국어로 작성한다. API, 클래스, 키, 오류 메시지 등 고유 명칭은 원문을 유지한다.
- 코드 리뷰는 요약보다 **결함, 보안 문제, 자원 누수, 회귀 가능성, 테스트 누락**을 심각도 순으로 먼저 제시한다.

## 검증

- 실행 가능한 명령 목록은 `docs/commands.md`를 따른다. 변경과 가장 좁게 관련된 검증부터 수행하고, 필요할 때 범위를 넓힌다.
- 빌드 성공을 실행 성공으로 간주하지 않는다. UI 변경은 `npm run tauri dev`나 `app/scripts/verify-*.mjs`로 직접 확인한다.
- 지문·해시 계산 경로를 건드렸다면 golden bit 테스트 통과가 검증의 최소 조건이다(`docs/invariants.md`).
- 검증을 실행하지 못했으면 실행하지 않았다고 명시하고, 이유와 사용자가 실행할 정확한 명령 또는 절차를 제공한다.
